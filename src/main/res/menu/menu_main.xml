<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:title="Paste"
        app:showAsAction="ifRoom"
        android:id="@+id/paste"
        android:icon="@drawable/paste_icon"/>
    <item
        android:title="Refresh"
        app:showAsAction="never"
        android:id="@+id/refresh"
        android:icon="@drawable/refresh_icon"/>
    <item
        android:title="New File"
        app:showAsAction="never"
        android:id="@+id/newfile"
        android:icon="@drawable/newfile_icon"/>
    <item
        android:title="New Folder"
        app:showAsAction="never"
        android:id="@+id/newfolder"
        android:icon="@drawable/newfolder_icon"/>
    <item
        android:title="Show hidden"
        app:showAsAction="never"
        app:actionViewClass="android.widget.CheckBox"
        android:checkable="true"
        android:id="@+id/showhidden" />

</menu>